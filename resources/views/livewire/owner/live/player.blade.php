<div id="video-player" class="mb-3" wire:ignore
    data-stream-url="{{ $url }}"
    data-state="{{ $state }}"
    data-text="{{ $offlineText }}"
    data-date="{{ $statusChangedAt }}"
    data-api-url="{{ $apiUrl }}">
</div>